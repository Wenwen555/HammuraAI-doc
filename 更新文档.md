# 更新文档

## 2026-05-08

- 为本项目安装产品经理场景前五个专用 skills，项目级落点为 `.agents/skills/`：
  - `brainstorming`，来源 `obra/superpowers`
  - `write-a-prd`，来源 `mattpocock/skills`；上游当前可安装名为 `to-prd`，本项目保留 `write-a-prd` 兼容别名目录
  - `writing-plans`，来源 `obra/superpowers`
  - `ab-test-setup`，来源 `coreyhaines31/marketingskills`
  - `analytics-tracking`，来源 `coreyhaines31/marketingskills`
- `skills-lock.json` 已记录安装来源、路径和 `write-a-prd` 本地别名说明。

## 2026-05-09

- 基于 `initial_plan/sow-fastgpt-rag-answer-api.md`、`initial_plan/fastgpt-legal-knowledge-base-agent-guide-2026-05-07.md` 和当前 `backend_app/` 现状，梳理第一阶段的双路线方案：
  - 路线 A：FastGPT 法律知识库展示 POC
  - 路线 B：自研 RAG/Answer API 底座 POC
- 明确两条路线都只覆盖第一阶段交付，不展开到权限治理、案件隔离、业务接入等第二阶段内容。
- 形成“模块 / 验收标准 / 建议报价”的报价表草案，用于后续范围确认与商务讨论。
- 新增 `mkdocs.yml` 与 `doc/` 文档站骨架，开始维护 HammuraAI 初步设计文档。
- 文档首页 `doc/index.md` 已展示当前立项进度与第一阶段交付产物。
- 新增模块页，覆盖：
  - 数据库搭建
  - 检索工具
  - 重排功能
  - LLM 调用模块
  - 用户档案管理
  - 多用户会话功能
  - Prompt 模板管理
- 新增法律风格的 MkDocs 自定义样式 `doc/styles/legal-theme.css`，整体排版定位为简洁、稳重、面向大众可读。
- 修正 MkDocs 静态样式路径：`extra_css` 从 `doc/styles/legal-theme.css` 改为 `styles/legal-theme.css`，避免页面可构建但样式引用失效。
- 复核 `mkdocs serve` 报错原因，当前已确认默认端口 `8000` 被本机其他进程占用；文档配置本身可正常 `build`，建议以自定义端口运行预览。
- 文档主题升级为 `mkdocs-material`，参考官方文档站的首页与导航风格重做 UI。
- 新增 Material 主题配置、双色模式切换、Hero 区块、卡片式进度面板以及法律风格图标 `doc/assets/scales.svg`。
- 当前 Material 版本可正常构建与预览；运行时会输出一条来自主题官方的 MkDocs 2.0 兼容性警告，但不影响本项目现阶段使用。
- 继续参考 Material 官网首页，去掉此前自定义金棕配色，改为更接近官网的白底与默认 indigo 交互色。
- 首页新增 `code annotations` 展示区：左侧代码块使用 Material 原生注释语法，右侧为说明文案与按钮，整体布局参考官网双栏展示方式。
- 进一步增强首页展示感：
  - 左侧代码展示区增加伪窗口栏、卡片阴影和更接近官网的浮层质感
  - 右侧说明区强化标题层级、留白和按钮节奏
  - 将第一阶段内容改写为一组可点击的 `showcase` 卡片区，而不是仅保留普通信息卡片
- 调整站点导航结构：
  - 桌面端隐藏左侧主目录占位
  - 在站点标题下方新增横向主目录条
  - 一级栏目支持展开显示子页面链接，以更大正文阅读区换取更轻量的导航干扰
- 新增 Material 主题 override：
  - `doc/overrides/main.html`
  - `doc/overrides/partials/horizontal-nav.html`
## 2026-05-09 - 横向导航浮层遮挡修复

- 将首页顶部横向导航从半透明磨砂背景调整为实体背景，避免正文内容在 sticky 悬浮条下方“漏字”。
- 提升横向导航层级并增加独立背景覆盖层，保证滚动时主内容不会视觉穿透导航区域。
- 保留横向导航的悬浮感，但收敛为更稳定的文档站风格，便于继续打磨 `mkdocs-material` 式首页布局。

## 2026-05-09 - 横向导航取消吸顶

- 将首页横向导航从 `sticky` 吸顶浮层改为普通页内导航带，避免正文标题在滚动时从导航下方穿过。
- 缩小导航带垂直内边距，减轻首页首屏的压迫感，让 Hero 与一级导航关系更接近 Material 文档首页。
- 保留横向展开式访问逻辑，但不再让该导航长期悬停在正文之上。

## 2026-05-09 - 横向导航改为 Hover 展开

- 将顶部目录从 `details / summary` 点击展开改为普通导航项 + 悬浮面板。
- 一级栏目现在支持鼠标移入自动展开、移出自动收回，交互更接近常见文档站导航。
- 保留 `focus-within` 行为，键盘聚焦时子菜单也可保持展开。

## 2026-05-09 - Hover 子菜单断层修复

- 修复一级导航按钮与下拉面板之间存在的 hover 空隙，避免鼠标下移点击子页面时菜单提前消失。
- 缩短面板与按钮之间的垂直距离，并为一级导航项补一段透明 hover 缓冲区，使指针可自然滑入子菜单。

## 2026-05-09 - 子页导航卡住修复

- 拆分顶部导航的“当前栏目高亮”和“下拉面板展开”状态，避免进入模块子页面后下拉菜单永久展开。
- 当前栏目仍保留高亮样式，但面板只在 hover 或键盘焦点进入时显示。

## 2026-05-09 - 集成 PG + pgvector 一体化 RAG 技术方案

- 将 PostgreSQL + pgvector 一体化 RAG 引擎方案写入 `doc/architecture.md`，明确第一阶段底座路线、设计原则和可退出机制。
- 更新 `doc/modules/database.md`，补充 `documents` / `document_chunks` 目标模型、`Vector(dim)` 字段、Alembic 启用 `vector` 扩展、HNSW 索引和标量过滤索引规范。
- 更新 `doc/modules/retrieval.md`，补充离线入库链路、在线检索链路、metadata filter、pgvector Top-K 与全文/关键词混合检索策略。
- 更新 `doc/modules/rerank.md`，补充向量召回、全文召回和业务规则偏置的融合排序策略。
- 更新 `doc/modules/llm.md`，补充检索上下文、引用结构、拒答约束与流式生成之间的协作关系。
- 同步 `doc/phase-one-deliverables.md` 和 `doc/index.md`，把 pgvector 目标模型、HNSW 索引设计和 PG 混合检索验证纳入第一阶段交付口径。

## 2026-05-09 - 新增 MkDocs GitHub Pages 发布工作流

- 新增 `.github/workflows/docs.yml`，在 `main` / `master` 分支 push 文档相关变更时自动构建并发布 MkDocs 文档站。
- 工作流使用 GitHub Pages 官方动作：`configure-pages`、`upload-pages-artifact`、`deploy-pages`。
- 构建步骤执行 `mkdocs build --strict --site-dir site`，用于在发布前拦截文档配置、链接和 Markdown 构建问题。
- 保留 `workflow_dispatch`，支持在 GitHub Actions 页面手动触发文档发布。
